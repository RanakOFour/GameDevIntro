local categoryName = "Drawable"
local baseAttributes = {
    shaderPath = "",
    shader = nil,
    texturePath = "",
    texture = nil,
    modelPath = "",
    model = nil
}

return Category.new(categoryName, baseAttributes)